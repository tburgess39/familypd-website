# FamilyPD PGS Contact Units Page Fix v31

This fixes the page shown in the screenshot:

`/learning-center/pgs-contact-units/`

It does not replace `/pgs/`.

Changed files only:
- learning-center/pgs-contact-units/index.html
- assets/css/styles.css

Upload both folders to the repository root and replace the matching files.
After deployment, test:
`https://familypd.org/learning-center/pgs-contact-units/?v=31`
