# Remove or Move These Files from the Public Website Repository

After uploading this correction, remove:

- `assets/js/workspace-config.js`
- `assets/js/workspace-client.js`
- `apps-script/familypd-workspace/`

Keep the Apps Script source in the Google Apps Script project, a separate private/source repository, or a secure local source package.

The `/workspace/` page should remain. This update replaces it with a transition page explaining that private workspace features are moving to `app.familypd.org`.
