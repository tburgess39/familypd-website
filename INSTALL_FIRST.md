# FamilyPD OS v10.3 — Base64 Download Repair

Replace only `Scripts.html` in the Apps Script project.

This repair safely handles:
- complete `data:` URLs,
- wrapped response objects,
- spaces/newlines introduced during transport,
- URL-safe Base64 (`-` and `_`), and
- missing Base64 padding.

Then save all files and deploy a new version of the existing web app.
